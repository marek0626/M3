[package]
name = "hashfile"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/hashfile.rs"
crate-type = ["staticlib"]

[dependencies]
m3 = { path = "../../../libs/rust/m3" }
pipecli = { path = "../../../libs/rust/pipecli" }

